{
  "name": "githubapi-monorepo",
  "private": true,
  "scripts": {
    "install": "cd app && npm install && cd ../server && npm install",
    "build": "npm run build:app && npm run build:server",
    "build:app": "cd app && npm run build",
    "build:server": "cd server && npm run build",
    "dev": "concurrently \"cd app && npm run dev\" \"cd server && npm run dev\"",
    "start": "npm run build && npm run start:app && npm run start:server",
    "start:app": "cd app && npm run start",
    "start:server": "cd server && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "i": "^0.3.7",
    "npm": "^11.3.0",
    "serverless-http": "^3.2.0"
  }
}
